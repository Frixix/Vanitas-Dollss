document.addEventListener("DOMContentLoaded", () => {
  console.log("Página lista para interacción");

  // Puedes agregar animaciones, sliders, formularios, etc. desde aquí.
});


document.addEventListener("DOMContentLoaded", () => {
  console.log("Página lista para interacción");

  const seccionValores = document.querySelector(".valores-pagina");
  if (seccionValores) {
    seccionValores.classList.add("animar-valores");
  }
});